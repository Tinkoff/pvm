name: 'setup'
description: ''
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v2
      with:
        node-version: '16'
        cache: yarn
    - run: yarn --immutable
      shell: bash
    # without this, strangely, git stash create begin failing without message
    - run: git diff
      shell: bash
