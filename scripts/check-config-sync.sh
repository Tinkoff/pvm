(npm run generate-config-schema && npm run check-no-staged-changes) || (echo "Config schema at \`packages/pvm-core/config-schema.json\` is not in sync with \`config-schema.ts\`. Please run \`npm run generate-config-schema\` and commit the result" && exit 1)
